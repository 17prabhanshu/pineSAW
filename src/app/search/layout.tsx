import type { Metadata } from "next";
export const metadata: Metadata = { title: "Global Search · pineSAW" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
