import type { Metadata } from "next";
export const metadata: Metadata = { title: "Intelligence Reports · pineSAW" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
